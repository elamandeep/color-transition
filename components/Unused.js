    {/* <AnimatePresence exitBeforeEnter>
            {!isLoading ? (
              <Layout value={router._key}>
                <Component {...pageProps} />
              </Layout>
            ) : (
              <Loader value="loader" setIsLoading={setIsLoading} />
            )}
          </AnimatePresence> */}